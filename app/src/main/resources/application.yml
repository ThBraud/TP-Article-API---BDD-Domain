app:
  persistence:
    type: jpa

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/db_tp
  datasource:
    url: jdbc:mysql://localhost:3306/db_movie?useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
